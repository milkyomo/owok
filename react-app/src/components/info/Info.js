import React, { useState } from "react";
import { useSelector } from "react-redux";

import "./Info.css";

const Info = () => {
  return (
    <div>
      <h1>info page</h1>
    </div>
  );
};

export default Info;
